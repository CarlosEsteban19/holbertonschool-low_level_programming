#include "main.h"

/**
 * add - adds two integers and returns result.
 * @a: 1st number
 * @b: 2nd number
 * Return: Result of a + b
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
