#include "main.h"

/**
 * mul - multiplies two integers and returns result.
 * @a: 1st number
 * @b: 2nd number
 * Return: Result of a + b
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
